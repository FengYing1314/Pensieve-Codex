interface:
  display_name: "Pensieve Sync to Main"
  short_description: "Sync explicitly requested language branches"
  default_prompt: "Use $pensieve-sync-to-main for the requested Pensieve language-branch sync, using the specified source and target."

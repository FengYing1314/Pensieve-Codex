interface:
  display_name: "Pensieve"
  short_description: "Structured project memory and workflow routing"
  default_prompt: "Use $pensieve to recall this project's knowledge and route the requested workflow."
policy:
  allow_implicit_invocation: true
